PROGRAM Prueba2Correcto ;

! Declaracion de variables
REAL :: r1, r2, r3 ;
CHARACTER (5) :: c1, c2, c3, c4, c5 = 'MAYOR' ;
CHARACTER (5) :: c6, c7, c8, c9, c10 = 'MENOR' ;
CHARACTER (5) :: c11, c12, c13, c14, c15 = 'IGUAL' ;
INTEGER :: num ;

! Declaracion de constantes
REAL , PARAMETER :: cte = 52.31 ;

INTERFACE
	SUBROUTINE DarValor ( Para1, Para2, Para3 )
		REAL, INTENT ( IN ) Para1 ;
		REAL, INTENT ( IN ) Para2 ;
		REAL, INTENT ( IN ) Para3 ;
	END SUBROUTINE DarValor

	FUNCTION Comparacion ( Para1, Para2, Para3, Comp)
		INTEGER :: Comparacion;
		REAL, INTENT ( IN ) Para1 ;
		REAL, INTENT ( IN ) Para2 ;
		REAL, INTENT ( IN ) Para3 ;
		REAL, INTENT ( IN ) Comp ;
	END FUNCTION Comparacion

END INTERFACE

CALL DarValor ( 25.45, 54.58, 154.24 ) ;
num = Comparacion ( r1, r2, r3, cte);

END PROGRAM Prueba2Correcto

SUBROUTINE DarValor ( Para1, Para2, Para3 )
	REAL, INTENT ( IN ) Para1 ;
	REAL, INTENT ( IN ) Para2 ;
	REAL, INTENT ( IN ) Para3 ;
	r1 = Para1 ;
	r2 = Para2 ;
	r3 = Para3 ;
END SUBROUTINE DarValor

FUNCTION Comparacion ( Para1, Para2, Para3, Comp)
	INTEGER :: Comparacion;
	REAL, INTENT ( IN ) Para1 ;
	REAL, INTENT ( IN ) Para2 ;
	REAL, INTENT ( IN ) Para3 ;
	REAL, INTENT ( IN ) Comp ;
	INTEGER :: i = 0 ;
	IF ( r1 > cte ) THEN 
		i = i + 1;
	ENDIF
	IF ( r2 > cte ) THEN 
		i = i + 1;
	ENDIF
	IF ( r3 > cte ) THEN 
		i = i + 1;
	ENDIF
	IF ( ( r1 > cte ) .AND. ( r2 > cte ) .AND. ( r3 > cte) ) THEN
		mensaje = "Todos los reales son 'mayores' que la constante" ;
	ELSE 
		mensaje = "No todos los reales son ""mayores"" que la constante" ;
	ENDIF
	Comparacion = i ;
END FUNCTION Comparacion